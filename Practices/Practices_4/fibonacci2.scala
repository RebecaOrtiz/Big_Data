//Create function 
def fibonacci(num:Int):Float=
{
//If the condition (if num is less than 2) is met the code will be executed
    if(num<2)
    {
//Returns the value of num          
        return num
    }
//If not     
        else
    { 
        fibona =(((1+Math.sqrt(5))/2))
        fibona2 =(((1-Math.sqrt(5))/2))/Math.sqrt(5))
    
    }
        return fibona2
}
fibonacci (13)
//The result is
//res2: Float = 233.0
