#We import the library with which we will work
import scala.math

#The value of the variable is defined
val bird = "tweet"

#The "s" in any string allows the use of variables directly within the string itself
#"$" is nested within the processed string of type s

println (s "I'm writing a, $ bird")

